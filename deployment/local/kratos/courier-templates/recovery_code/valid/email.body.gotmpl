<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #f5f5f5; padding: 20px; border-radius: 5px; margin-bottom: 20px; }
        .content { margin: 20px 0; }
        .code-box { background-color: #e8f4f8; padding: 15px; border-radius: 5px; text-align: center; margin: 20px 0; }
        .code { font-size: 32px; font-weight: bold; letter-spacing: 5px; color: #0066cc; }
        .vehicle-list { margin: 20px 0; padding: 10px; background-color: #f9f9f9; border-left: 4px solid #0066cc; }
        .vehicle-item { margin: 10px 0; }
        .footer { margin-top: 30px; padding-top: 20px; border-top: 1px solid #ddd; font-size: 12px; color: #666; }
    </style>
</head>
<body>
    <div class="container">
        {{- if .Identity.metadata_public.isInvitation }}
        <div class="header">
            <h2>Welcome to Classics Chain!</h2>
            <p>You've been invited to join our platform to manage your classic vehicles.</p>
        </div>

        <div class="content">
            {{- if .Identity.metadata_public.invitationUrl }}
            <p style="margin-bottom: 20px;">
                <a href="{{ .Identity.metadata_public.invitationUrl }}" style="display: inline-block; padding: 10px 20px; background-color: #2563eb; color: white; text-decoration: none; border-radius: 4px; font-weight: 500;">
                    Complete Your Signup
                </a>
            </p>
            <p style="text-align: center; color: #666; margin: 15px 0;">Or enter this code manually:</p>
            {{- else }}
            <p>To complete your signup and start managing your vehicles, please enter the following code:</p>
            {{- end }}

            <div class="code-box">
                <div class="code">{{ .RecoveryCode }}</div>
            </div>

            {{- if .Identity.metadata_public.vehicles }}
            <div class="vehicle-list">
                <p><strong>You've been invited to manage:</strong></p>
                {{- range .Identity.metadata_public.vehicles }}
                <div class="vehicle-item">
                    • {{ .year }} {{ .make }} {{ .model }}{{ if .licensePlate }} ({{ .licensePlate }}){{ end }}
                </div>
                {{- end }}
            </div>
            {{- end }}

            <p>Once you enter this code, you'll be able to set up your password and access your account.</p>
        </div>
        {{- else }}
        <div class="header">
            <h2>Password Reset Request</h2>
            <p>We received a request to reset your Classics Chain password.</p>
        </div>

        <div class="content">
            <p>To reset your password, please enter the following code:</p>

            <div class="code-box">
                <div class="code">{{ .RecoveryCode }}</div>
            </div>

            <p>If you didn't request this password reset, you can safely ignore this email.</p>
        </div>
        {{- end }}

        <div class="footer">
            <p>This is an automated message. Please do not reply to this email.</p>
            <p>&copy; Classics Chain. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
